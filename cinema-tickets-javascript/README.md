# Cinema Tickets
A solution to the DWP cinema tickets test

## Setup
To start, clone the repository and run npm install. You can then run the tests and the linter:
```
npm install
npm run test
npm run lint
```

## Tests
The tests are only for the src/TicketService.js file. There should be 100% code coverage as calculated by the c8 library
